# 学习指南：从 Java 到 Agent 开发

> 作者：renbohao888（本项目作者）
> 目标：把已有的 **Java 后端基础** 平滑迁移到 **Agent 开发（Python 主流栈）**

---

## 一、为什么 Agent 开发用 Python？

- **生态最全**：LangChain、LangGraph、OpenAI SDK、smolagents、HuggingFace 全都以 Python 为第一语言。
- **招人也是 Python 为主**：Agent 岗位 JD 里几乎必写 Python。
- **上手快**：语法简洁，适合快速验证想法。

结论：**语言 = Python**。你的 Java 基础不会白学，思想通用。

---

## 二、开发环境（本项目已全部配好）

| 概念 | Java 里对应 | 本项目 | 说明 |
|------|------------|--------|------|
| 解释器/编译器 | JDK | Python 3.14 | Python 是解释执行 |
| 依赖管理 | Maven/Gradle(`pom.xml`) | `requirements.txt` + `pip` | 装库 |
| 虚拟环境 | 无(靠依赖隔离) | `.venv` | 隔离项目依赖 |
| 配置 | `application.yml` | `.env` + 环境变量 | API Key 在这 |
| 代码编辑 | IDEA | VS Code | 已装好 |
| 包结构 | `src/main/java/...` | `agent/` 目录 | 即“包” |

---

## 三、Python vs Java 对照表（重点，帮你跳过弯路）

| 事项 | Java | Python |
|------|------|--------|
| 变量 | `int a = 1;` | `a = 1` |
| 函数 | `public int add(int a,int b){...}` | `def add(a, b): ...` |
| 类 | `class A{ public void m(){} }` | `class A: def m(self): ...` |
| 注释 | `//` 和 `/* */` | `#` 和 `""" """` |
| 语句结束 | 必须 `;` | **不需要**（换行即结束） |
| 缩进 | 花括号 `{}` | **用缩进分层**（4 空格） |
| 接口/继承 | `implements/extends` | 简单继承：`class B(A):`，鸭子类型 |
| 集合 | List/Map | list / dict |
| 循环 | `for(int i...;` | `for x in ...:` |
| 异常 | `try-catch-finally` | `try-except-finally` |
| 包导入 | `import com.x.Y` | `from a import b` |
| 能跑即入口 | `public static void main` | `if __name__ == "__main__":` |
| 类型 | 强类型 | 动态类型（可不写类型注解） |

> 一句话：Python 少写很多“样板代码”，但你依然要懂 OOP 思想（类就是这样用的）。

---

## 四、本项目：四模块 与 Java 类比（最核心！）

把这个 Agent 想成一个 **Java 后端项目**，你会瞬间懂：

| Agent 模块 | 本项目文件 | Java 类比 |
|-----------|-----------|-----------|
| 🧠 大脑 LLM | `agent/graph.py` 的 `agent` 节点 | 一个“会思考的 Controller/Service”，调用大模型 API |
| 💾 记忆 | `agent/state.py` + `agent/memory.py` | 一个 **DTO(数据对象)** + **数据库(缓存)**，state=短期，sqlite=长期 |
| 🗺️ 规划 | `agent/graph.py` 的 `planner` 节点 | 一个“任务调度器”，先拆步骤 |
| 🔧 实施/工具 | `agent/tools.py` | 一组 **@Service 方法**，`@tool`≈`@Service`，供模型调用 |

**工作流 = 状态机编排**（类似 Java 里的状态机/工作流引擎）：
```
开始 → 规划(planner) → 大脑决策(agent) → 要调工具? → 执行(tools) → 回大脑 … → 输出
```

### 代码怎么对应 Java 概念：
- `state.py` 定义 `AgentState`（一个 class），就是存放数据的 **DTO**——里面有 `messages`（历史消息，短期记忆）和 `plan`（规划结果）。
- `tools.py` 里每个 `@tool` 装饰的函数，就是**可被模型远程调用的“Service 方法”**。
- `memory.py` 用 SQLite 把状态存进本地文件，就像你 Java 里连 MySQL/Redis 做**持久化**。
- `graph.py` 用 `StateGraph` 把节点连起来，像是**工作流引擎**，`add_edge` 决定顺序和条件分支。
- `main.py` 的 `while True` + `input()` 就是**命令行入口**，相当于一个交互式后端终端。

---

## 五、怎么跑通（已给你写好）

```bash
# 1. 进项目目录
cd langgraph-agent

# 2. 激活虚拟环境（相当于用对 JDK）
.venv\Scripts\activate

# 3. 填 Key：编辑 .env，把 OPENAI_API_KEY 换成你的真 Key

# 4. 运行
.venv\Scripts\python -m agent.main
```
看到 `你:` 后，试试：`帮我算 (3+8)*2`。

---

## 六、学习路线（建议按顺序，约 2~4 周入门）

1. **先跑通**（1天）：改 `.env` → 运行 → 理解四模块输出。
2. **学 Python 基础**（1~2周）：变量/函数/类/列表字典/循环/异常/文件读写。用你会的 Java 类比。
3. **理解 LangGraph 三个概念**（2~3天）：**State(状态)** / **Node(节点)** / **Edge(边)**——就是我们图里的节点和连线。
4. **加自己的工具**（2~3天）：在 `tools.py` 加一个函数 `@tool` 装饰并注册进 `TOOLS`。
5. **读官方教程**：langchain 官方文档 + LangGraph 快速上手。
6. **做一个自己的小作品**：把包名改成 `my_agent`，加联网搜索或人设，写进 README。

---

## 七、必读文档（都在官网，免费）

- LangGraph 官方：https://docs.langchain.com/oss/python/langgraph/
- LangChain Academy 入门课：https://academy.langchain.com/
- Python 入门：https://docs.python.org/zh-cn/3/tutorial/

---

> 小提醒：`tools.py` 里的 `calculator` 用了 `eval`，但已过滤字符，仅用于教学演示。真实生产环境的工具函数要放进 Docker/E2B 之类沙箱里跑，不要直接执行不可信代码。
